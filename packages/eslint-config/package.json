{
  "name": "@wormarket/eslint-config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./index.js"
  },
  "scripts": {
    "build": "echo \"No build required for @wormarket/eslint-config\"",
    "lint": "eslint .",
    "format": "prettier --check . --ignore-path ../../.prettierignore",
    "format:write": "prettier --write . --ignore-path ../../.prettierignore",
    "typecheck": "echo \"No typecheck required for @wormarket/eslint-config\"",
    "test": "echo \"No tests configured for @wormarket/eslint-config\""
  }
}

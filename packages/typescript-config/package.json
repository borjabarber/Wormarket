{
  "name": "@wormarket/typescript-config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    "./base": "./base.json",
    "./library": "./library.json",
    "./next": "./next.json",
    "./nest": "./nest.json"
  },
  "scripts": {
    "build": "echo \"No build required for @wormarket/typescript-config\"",
    "lint": "prettier --check . --ignore-path ../../.prettierignore",
    "format": "prettier --check . --ignore-path ../../.prettierignore",
    "format:write": "prettier --write . --ignore-path ../../.prettierignore",
    "typecheck": "echo \"No typecheck required for @wormarket/typescript-config\"",
    "test": "echo \"No tests configured for @wormarket/typescript-config\""
  }
}
